{
  "name": "repowiki",
  "version": "0.1.0",
  "description": "Generate a deterministic Markdown wiki for software repositories.",
  "type": "module",
  "bin": {
    "repowiki": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "pretest": "npm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "fast-glob": "^3.3.2",
    "simple-git": "^3.27.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
